/* 根据实际导出的资源与几何生成工程交接页，不手工维护第二份坐标。 */
'use strict';
const fs=require('node:fs'),path=require('node:path');
const esc=value=>String(value??'').replaceAll('&','&amp;').replaceAll('<','&lt;').replaceAll('>','&gt;').replaceAll('"','&quot;');
const size=n=>(n/1024).toFixed(1)+' KiB';
function writePages(root,base,{manifest:m,screens,fonts,contract:c}){
  function document(prefix,portable){
    const link=(relative,label)=>{
      if(typeof relative!=='string'||!relative.length)throw new Error('交接资源链接缺少路径：'+label);
      return '<a href="'+prefix+relative+'">'+esc(label)+'</a>';
    };
    const files=m.resources;
    const resourceTable=files.map(r=>'<tr><td><img class="asset" src="'+prefix+r.encodings.png.path+'" alt="'+esc(r.label)+'"><code>'+esc(r.id)+'</code></td><td>'+r.width+' × '+r.height+'<br>'+esc(r.kind==='apple_reference'?'Apple 参考原图同源':'项目绘制／背景')+'</td><td>'+link(r.encodings.png.path,'PNG')+' · '+(r.vector?link(r.vector.path,'SVG'):r.original_path?link(r.original_path,'原文件'):'页面样式导出')+'<br>'+link(r.encodings.rgb565a8.path,'RGB565＋A8')+' / '+link(r.encodings.bgra8888.path,'BGRA8888')+(r.encodings.rgb565?'<br>'+link(r.encodings.rgb565.path,'不透明 RGB565'):'')+'</td><td>'+size(r.encodings.rgb565a8.bytes)+'<br><span class="muted">许可待核查，不自动发布</span></td></tr>').join('\n');
    const grid=screens.find(s=>s.id==='System.grid');
    const gridRows=grid.controls.map((cell,n)=>{
      const placement=grid.resource_placements.find(r=>r.parent_key===cell.key);
      const res=files.find(r=>r.id===placement?.asset_id);
      return '<tr><td>'+(n+1)+' · '+esc(cell.label)+'</td><td>'+cell.center_x+', '+cell.center_y+'</td><td>'+cell.width+'</td><td>'+(res?link(res.encodings.png.path,res.id):'缺少资源')+'</td><td>'+esc(c.grid_routes[cell.label]||'明确演示／未接入')+'</td></tr>';
    }).join('\n');
    const annotated='<svg viewBox="0 0 390 450" class="reference-svg" role="img" aria-label="蜂窝位置编号图"><image href="'+prefix+grid.reference.path+'" width="390" height="450"/>'+grid.controls.map((cell,n)=>'<circle cx="'+cell.center_x+'" cy="'+cell.center_y+'" r="'+cell.radius+'" fill="none" stroke="#ffea8a" stroke-width="1" stroke-dasharray="3 2"/><rect x="'+(cell.center_x-10)+'" y="'+(cell.center_y-10)+'" width="20" height="20" rx="6" fill="#080c13" stroke="#cfe3ff"/><text x="'+cell.center_x+'" y="'+(cell.center_y+4)+'" fill="white" text-anchor="middle" font-family="sans-serif" font-size="12">'+(n+1)+'</text>').join('')+'</svg>';
    const screenSections=screens.map((s,n)=>{
      const textRows=s.texts.filter(t=>!t.text.match(/^\d\d$/)||['06','45'].includes(t.text)).slice(0,16).map(t=>'<tr><td>'+esc(t.text)+'<br><code>'+t.parent_key+'</code></td><td>'+t.x+', '+t.y+'<br>'+t.width+' × '+t.height+'</td><td>'+esc(t.style.fontSize)+' / '+esc(t.style.fontWeight)+'<br>字距 '+esc(t.style.letterSpacing)+'<br>行高 '+esc(t.style.lineHeight)+'</td><td>'+esc([...new Set(t.actual_fonts.map(f=>f.familyName))].join(' / ')||'见字体报告')+'<br>基线 '+(t.baseline_y??'需目标字体校准')+'</td></tr>').join('');
      const componentRows=s.components.map(x=>'<tr><td><code>'+esc(x.key)+'</code> '+esc(x.class)+'</td><td>'+x.x+', '+x.y+'</td><td>'+x.width+' × '+x.height+'</td><td>'+esc(x.style.borderRadius)+'<br>'+esc(x.style.backgroundColor)+'</td></tr>').join('');
      return '<section id="screen-'+n+'"><p class="eyebrow">SCREEN '+(n+1)+' / '+esc(s.id)+'</p><h2>'+esc(s.name)+'</h2><div class="screen-detail"><div class="reference-frame"><img src="'+prefix+s.reference.path+'" width="390" height="450" alt="'+esc(s.name)+'已批准基准"></div><div><p class="notice">'+esc(s.runtime_contract)+'</p><h3>必须对齐</h3><ul>'+s.must_match.map(x=>'<li>'+esc(x)+'</li>').join('')+'</ul><h3>工程行为验收</h3><p>'+s.states_to_verify.map(esc).join(' · ')+'</p><p>'+link(s.spec.path,'完整几何、文字、资源引用与动作映射 JSON')+'</p>'+s.scroll_areas.map(a=>'<p>滚动区 <code>'+a.key+'</code>：('+a.x+', '+a.y+', '+a.width+', '+a.height+')；内容高 '+a.scroll_height+'，最大滚动 '+a.max_scroll_y+' px。</p>').join('')+s.scroll_end_references.map(a=>'<p>'+link(a.path,'查看滚动到末端的目标图')+'</p>').join('')+'</div></div><details><summary>组件尺寸和局部样式</summary><div class="scroll"><table><thead><tr><th>标识 / 类</th><th>左上角 x,y</th><th>宽 × 高</th><th>圆角 / 背景</th></tr></thead><tbody>'+componentRows+'</tbody></table></div><p class="muted">渐变、描边、阴影、透明度、剪裁和子节点见该页 JSON；不能只取背景色而丢掉材质。</p></details><details><summary>文字尺寸与实际浏览器字体</summary><div class="scroll"><table><thead><tr><th>样例文字</th><th>文本范围 x,y / w,h</th><th>字号 / 字重</th><th>实际字体 / 基线 y</th></tr></thead><tbody>'+textRows+'</tbody></table></div><p class="muted">这里是文本测量范围，不是直接传给 LVGL 的控件框。控件框取组件规格；范围和基线用于对照。完整文字、Canvas 度量及字体详情在 font-spec.json。未取得可靠基线的项明确为空，不伪造数值。</p></details></section>';
    }).join('\n');
    return '<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>V00 同源设计资源与工程交接 v1</title><style>'+
      '*{box-sizing:border-box}html{scroll-behavior:smooth;scroll-padding-top:85px;color-scheme:dark}body{margin:0;background:#0c1016;color:#e7edf7;font:15px/1.75 "Segoe UI","Microsoft YaHei UI",sans-serif}a{color:#93caff}code{font:12px/1.7 Consolas,monospace;overflow-wrap:anywhere;color:#c4e4ff}header{position:sticky;top:0;z-index:10;background:#0c1016f7;border-bottom:1px solid #2d394a;padding:15px max(22px,calc((100vw - 1240px)/2));display:flex;justify-content:space-between;gap:20px;align-items:center}header strong{white-space:nowrap}header nav{display:flex;gap:18px;flex-wrap:wrap;font-size:12px}main{max-width:1280px;padding:44px 25px 80px;margin:auto}h1{font-size:40px;line-height:1.25;margin:12px 0 20px;letter-spacing:-1px}h2{font-size:26px;line-height:1.4;margin:9px 0 20px}h3{font-size:17px;margin:20px 0 8px}.eyebrow{font-size:11px;letter-spacing:2px;color:#80d5bb}.lead{max-width:900px;color:#b7c5d9;font-size:17px}.notice{padding:16px 19px;border-left:3px solid #64b8ff;background:#132134;border-radius:0 9px 9px 0}.warning{border-color:#ffd471;background:#2a2519}.stats{display:grid;grid-template-columns:repeat(4,1fr);gap:14px;margin:28px 0}.stats div{padding:16px;background:#17202d;border:1px solid #304056;border-radius:11px}.stats b{display:block;font-size:28px}.stats span,.muted{color:#a6b4c8;font-size:12px}.links{display:flex;gap:12px;flex-wrap:wrap}.links a{background:#1a2b41;border:1px solid #395574;padding:8px 13px;border-radius:8px;font-size:13px}section{padding-top:44px;margin-top:25px;border-top:1px solid #2a3547}.split,.screen-detail{display:grid;grid-template-columns:390px minmax(0,1fr);gap:35px;align-items:start}.reference-frame{width:390px;max-width:100%;background:#000;border:1px solid #263248;overflow:hidden;border-radius:16px}.reference-frame img,.reference-svg{display:block;width:100%;height:auto}.screen-detail>div{min-width:0}table{width:100%;border-collapse:collapse;font-size:13px}th,td{border:1px solid #2b394d;text-align:left;padding:11px 13px;vertical-align:top}th{background:#1b2a3e}td img.asset{display:block;width:68px;height:68px;object-fit:contain;margin:3px 0 8px;background:#141c28;border-radius:10px}.scroll{overflow:auto;max-width:100%;margin:18px 0}.scroll table{min-width:680px}details{border:1px solid #2e3c50;background:#111925;border-radius:10px;margin:18px 0;padding:15px 18px}summary{cursor:pointer;color:#d0e5ff;font-weight:600}.flow{border:1px solid #345c54;padding:15px 18px;border-radius:10px;background:#132922}.asset-count{font-size:13px;color:#aabed7}.action-list li{margin:9px 0}pre{white-space:pre-wrap;overflow-wrap:anywhere;padding:18px;background:#07111c;border:1px solid #294259;border-radius:9px}.tile-gallery{display:grid;grid-template-columns:repeat(3,1fr);gap:15px;margin-top:26px}.tile-gallery a{display:block;background:#151e2b;padding:13px;border-radius:10px;text-decoration:none}.tile-gallery img{width:100%;height:auto;display:block;border-radius:10px}.tile-gallery span{display:block;margin:8px 0;font-size:13px}@media(max-width:900px){.split,.screen-detail{grid-template-columns:1fr}.reference-frame,.reference-svg{max-width:390px}.tile-gallery{grid-template-columns:repeat(2,1fr)}header{position:static;align-items:start}.stats{grid-template-columns:repeat(2,1fr)}}@media(max-width:480px){main{padding:26px 15px}h1{font-size:29px}header nav{gap:7px 14px}header{padding:15px}.stats{gap:8px}.tile-gallery{grid-template-columns:1fr}.reference-frame{width:100%}section{padding-top:30px}.notice{padding:12px 14px}}'+
      '</style></head><body><header><strong>iwatch / V00 设计交接</strong><nav><a href="#start">先从这里开始</a><a href="#grid">蜂窝定位表</a><a href="#assets">图标资源</a><a href="#fonts">字体与色彩</a><a href="#acceptance">验收</a></nav></header><main>'+
      '<p class="eyebrow">SOURCE-MATCHED ASSETS / 390 × 450 / DESIGN HANDOFF 01</p><h1>使用同一套图标。<br>按同一份尺寸落地。</h1><p class="lead">六页批准稿的素材、尺寸、文字度量与验收要求已放在一起。图标从原始输入导出，几何来自实际页面测量；这不是重新设计的替代稿，也不是已经通过的固件。</p>'+
      '<p class="notice warning"><strong>发布边界：</strong>包含 Apple 官方参考图片的同源副本，未取得再分发许可；不自动导入公开固件、不自动推送。本包未复制 Windows 或 Apple 字体。参考和项目绘制素材均记录来源，许可与字体替代仍是独立准入项。</p>'+
      '<div class="stats"><div><b>6</b><span>页面规格与目标图</span></div><div><b>'+m.counts.assets+'</b><span>同源资源变体</span></div><div><b>17</b><span>蜂窝图标精确定位</span></div><div><b>'+m.counts.apple_references+'</b><span>Apple 参考变体，许可待查</span></div></div>'+
      '<div class="links">'+link('manifest.json','资源清单与校验')+link('contract.json','工程契约')+link('font-spec.json','字体规格')+link('color-tokens.json','色彩令牌')+link('validation.json','设计包校验记录')+(portable?'':'<a href="../assets/V00_设计资源包_v1.zip">下载便携资源包 ZIP</a><a href="Apple_Watch_UI实现计划_v3.html">实施计划 v3</a><a href="Apple_Watch全量UI审阅台_v2.html">已批准审阅台</a>')+'</div>'+
      '<section id="start"><h2>先接蜂窝页，确认后再铺开</h2><p class="flow">本设计包 → 工程师接入 <strong>System.grid</strong> → 同输入对照、架构复核与你确认 → 其余五页 → 主机 / 双工具链 / 获批后的板验</p>'+
      '<ol class="action-list"><li>保留现有业务、路由、输入取消与 OOM 处理，只替换视觉资源与几何；不要覆盖工程师已有修改。</li><li>蜂窝的每个图标使用下表对应文件，不再以信息圆圈、九点、时钟等占位符替换电话、消息、活动、照片等 App 身份。</li><li>PNG 与 SVG 是素材母版；raw 是格式明确的像素载荷，不是带 LVGL header 的成品 .bin。工程师负责导入容器/描述符及目标支持验证。</li><li>只接入选用的一种编码，保留文件 ID、尺寸、哈希；缺资源应明确报错或显示审定占位，不静默换成另一个图标。</li><li>公开发布前先解决素材许可。若必须换素材或字体，提交替代稿与你确认，不直接当成原稿已还原。</li></ol>'+
      '<p class="muted">本次只交付设计资源与校验工具，校验仅生成报告和对照图；保留工程师在制固件，未生成目标镜像、未连接串口/J-Link、未推送。</p>'+
      '<div class="tile-gallery">'+screens.map((s,n)=>'<a href="#screen-'+n+'"><img src="'+prefix+s.reference.path+'" width="390" height="450" alt="'+esc(s.name)+'基准"><span>'+esc(s.id)+' · '+esc(s.name)+'</span></a>').join('')+'</div></section>'+
      '<section id="grid"><h2>蜂窝：不是十七个同样大的彩色圆</h2><div class="split"><div>'+annotated+'</div><div><p>图标的中心、直径、内容和色彩独立固定。坐标原点是屏幕左上角，圆形命中使用同一中心和半径，不用包围矩形四角。</p><p>全量图标样片只证明外观，不能把 17 个 App 都标成 READY。当前只有设置、计时器、秒表和闹钟四个真实入口；其他入口需要明确演示/未接入边界。</p><p>默认几何按下表；批准稿的放大展示为 <code>scale(1.16)</code>。本包不重写既有手势状态机或批准新倍率范围。缩放、平移、取消和返回恢复仍需输入回归。</p><p>'+link(grid.spec.path,'蜂窝完整机器规格')+'</p></div></div><div class="scroll"><table><thead><tr><th>编号 / 图标</th><th>中心 x,y</th><th>直径 px</th><th>准确资源文件</th><th>当前真实入口</th></tr></thead><tbody>'+gridRows+'</tbody></table></div></section>'+
      '<section id="assets"><h2>可以交给工程师导入的资源</h2><p>原 PNG 保留不变；渲染尺寸版本只执行批准稿的缩放与 CSS 滤色。项目 SVG 来自批准稿的同一条路径，不手工近似重画。单色图符可由工程师进一步选择 A8 遮罩，但必须保留当前轮廓与抗锯齿。</p><p class="notice">推荐的图标/图符 RGB565＋A8 原始载荷合计 <strong>'+size(m.budget.recommended_icon_rgb565a8_bytes)+'</strong>；可选控制中心纯背景 RGB565 为 <strong>'+size(m.budget.optional_background_rgb565_bytes)+'</strong>。刻度装饰另算；这些不包含现有固件资源、段头、对齐和解码缓存，不能当作固件预算已经通过。</p>'+
      '<details><summary>像素格式、透明边缘与导入规则</summary><ul><li>PNG：高精度可视母版，有透明度时保留 alpha，不先铺黑底再缩放。</li><li>BGRA8888：B,G,R,A 紧密字节序，对应 little-endian 逻辑字 0xAARRGGBB，非预乘 alpha。</li><li>RGB565＋A8：先 RGB565 小端颜色平面（每行宽×2），后 A8 平面（每行宽）；不等于所有目标驱动都已支持。</li><li>只有纯背景另给无 alpha 的 RGB565。所有 raw 文件无资源头；工程师验证 LVGL/EPIC 格式、stride、alpha 与刷屏路径后再接入。</li><li>同一图标不要把原图、PNG、两种 raw 全部链接；资源上限继续 1,048,576 B，不自动上调。</li></ul></details><div class="scroll"><table><thead><tr><th>素材 / 稳定 ID</th><th>尺寸 / 来源</th><th>原件与导入载荷</th><th>RGB565＋A8 大小 / 许可</th></tr></thead><tbody>'+resourceTable+'</tbody></table></div></section>'+
      '<section id="fonts"><h2>字体和材质也必须对齐</h2><p>当前浏览器实际使用：<strong>'+fonts.map(esc).join('、')+'</strong>。这些字体文件没有放进资源包，不能把 Windows/Apple 字库直接复制进公开固件。</p>'+
      '<p>现有固件 DroidSansFallback 是工程候选，不等同审阅稿字体。每个文字片段已记录字号、字重、字距、行高、颜色、实际字体、文本范围，以及能可靠测得的基线。工程师需制作数字与中文样条，让你确认替代字形；不能靠改变字号和挤压布局掩盖差异。</p>'+
      '<p><strong>配色优先级：</strong>页面具体渐变/局部色 → 该控件状态 → 通用色彩令牌。控制中心蓝色不是直接用全局蓝涂一个实心胶囊；月亮是原图轮廓，不是圆环。字体缺口、颜色量化、真实数据差异分别登记。</p><p><strong>RGB565：</strong>送屏保持现有格式。高精度素材可改善转换过程，但最终仍有 16 位色阶限制；本包提供 RGBA 母版与 565A8 载荷，是否采用抖动或其他转换须另做实屏对照。</p></section>'+
      screenSections+
      '<section id="acceptance"><h2>验收：对照图要能解释每一处差异</h2><ol class="action-list"><li>固定 390×450、默认缩放、同一页状态；基准图是静态样例，不是真实服务数据。比较布局时固定测试 Provider；正常模式坚持真实数据。</li><li>图标身份替代为零；可见元素遗漏为零；几何与裁切按测量值，整数化后的偏差不超过 1 px。抗锯齿和字体栅格不以全屏逐像素相等判定。</li><li>按图标、文字、布局、材质、状态五类列差异；“像素差均值较低”不能抵消一个错误图标、漏掉的操作或越界标签。</li><li>六页保留目标图 / 固件主机图 / 50% 叠图；蜂窝先验图标身份与 17 个位置，再验证真实四入口、拖动释放、双击、取消和返回。</li><li>没有电量或连接能力时使用未知/不可用，不要求复制样例 96% 或在线图标；这些是合理的业务差异，必须有说明，不能伪造成功。</li><li>主机通过后独立记录 GCC/Keil 身份与资源预算；实屏、手指触摸和按键仍需另获批准后验收。未完成许可、字体或板验时，不写 V00 最终关闭。</li></ol>'+
      '<h3>工程师回报模板</h3><pre>批次：V00 / System.grid 先行\n设计包：IW-V00-DESIGN-HANDOFF-1\n输入：manifest.json 哈希、图标 IDs、选用编码与资源预算\n实现：源码提交/工作树状态、真实四入口、其他图标能力策略\n视觉：基准 / 主机帧 / 叠图；图标/字体/几何/材质/状态差异逐项说明\n主机：测试命令、实际结果、输入取消/OOM/生命周期\n目标构建：GCC 与 Keil 各自身份/预算，未执行就写未执行\n实机：本批未授权则写未执行\n阻断：素材许可、字体替代及其他欠项</pre>'+
      '<p>'+link('manifest.json','完整资源清单')+' · '+link('font-spec.json','完整字体记录')+(portable?'':' · <a href="Apple_Watch_UI实现计划_v3.html#first">返回 V00 计划</a> · <a href="../../index.html">项目首页</a>')+'</p><p class="muted">来源基线：'+esc(m.approval_id)+'；导出 '+esc(m.generated_at)+'。所有“通过”需说明是设计包校验、主机、目标构建还是实机，不互相替代。</p></section></main></body></html>';
  }
  fs.writeFileSync(path.join(root,base,'index.html'),document('',true));
  fs.writeFileSync(path.join(root,'docs/ui/V00_设计资源与工程交接_v1.html'),document('../assets/v00-design-handoff/',false));
}
module.exports={writePages};
