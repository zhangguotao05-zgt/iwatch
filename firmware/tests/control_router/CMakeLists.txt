cmake_minimum_required(VERSION 3.20)
project(control_router_repair LANGUAGES C)
set(IW_TEST_TARGET_VIEW ON CACHE BOOL "真实 TARGET 分支" FORCE)
set(BASE "${CMAKE_CURRENT_SOURCE_DIR}/../d07_tiny_ttf_oom")
add_subdirectory("${BASE}" legacy)
# 沿用 D 三组诊断的有状态边界，源码由当前工作副本原样提取。
get_target_property(SOURCES test_tiny_ttf_oom SOURCES)
list(FILTER SOURCES EXCLUDE REGEX "(^|/)(test_router|test_tiny_ttf_oom|test_target_view)\\.c$")
set_property(TARGET test_tiny_ttf_oom PROPERTY SOURCES "${SOURCES}")
target_sources(test_tiny_ttf_oom PRIVATE router_model.c entry.c target_model.c)
target_include_directories(test_tiny_ttf_oom PRIVATE "${BASE}" "${CMAKE_CURRENT_BINARY_DIR}")
function(extract_function path marker output)
    file(READ "${path}" text)
    string(FIND "${text}" "${marker}" begin)
    if(begin LESS 0)
        message(FATAL_ERROR "Function marker missing: ${marker}")
    endif()
    string(SUBSTRING "${text}" ${begin} -1 rest)
    string(FIND "${rest}" "\n}" end)
    math(EXPR length "${end} + 2")
    string(SUBSTRING "${rest}" 0 ${length} body)
    file(WRITE "${CMAKE_CURRENT_BINARY_DIR}/${output}" "${body}")
endfunction()
extract_function("${SDK_ROOT}/middleware/app_fwk/gui_app_fwk.c" "void gui_app_remove_page(" sdk_remove.inc)
extract_function("${CMAKE_CURRENT_SOURCE_DIR}/../../iwatch/src/gui_apps/watch_demo.c" "static bool input_signal(" input_signal.inc)
